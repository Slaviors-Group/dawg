# DAWG Browser Extension

This directory contains the source code for the DAWG Browser Extension (Manifest V3). 
This extension is responsible for capturing DOM events (via `rrweb`) and network traffic directly from the user's browser, eliminating the need to launch an isolated Playwright browser for capture.
